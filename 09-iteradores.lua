array = {"Lua", "Tutorial"} 

for key,value in ipairs(array) -- ele salva três valores: uma função iterativa, constantes do estado, variáveis ​​de controle.
do
   print(key, value)
end
-- a função retorna o valor iteração para a lista de variáveis.
